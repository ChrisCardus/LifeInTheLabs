package com.events;

/**
 * The interface Event is for creating a structure that we can create multiple in-game com.events from.
 * @author Olivia Breen
 */
public interface Event {
	
	public Object[] GetEvent(int location);
}
